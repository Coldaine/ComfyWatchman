# Environment variable template for ComfyFixerSmart
# Copy this file to .env and uncomment/modify values as needed.
# These override values from config/default.toml.
# Required: Set your Civitai API key to download models.

# Civitai API Key (REQUIRED for downloading models from Civitai)
# Get your key from: https://civitai.com/user/account
CIVITAI_API_KEY=your_civitai_api_key_here

# Paths (override defaults if your setup differs)
# ComfyUI installation root directory
COMFYUI_ROOT=/path/to/your/ComfyUI

# Output directory for generated files (missing_models.json, etc.)
OUTPUT_DIR=./output

# Log directory for run logs and search history
LOG_DIR=./log

# State directory for download tracking
STATE_DIR=./state

# API Settings
# Timeout for Civitai API requests (seconds, default: 30)
CIVITAI_API_TIMEOUT=30

# Validation Thresholds
# Minimum file size for valid models (bytes, default: 1000000 for 1MB)
MIN_MODEL_SIZE=1000000

# Hours to consider a failed download "recent" (skip re-attempt, default: 1)
RECENT_ATTEMPT_HOURS=1

# Usage Notes:
# - Place this as .env in the project root.
# - The scripts will automatically load these variables.
# - For paths, use absolute paths to avoid issues.
# - CIVITAI_API_KEY is used directly in scripts for authentication.
# - Restart scripts after changing .env.